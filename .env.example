# Server-side (Express). Never expose these to the frontend.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
PORT=3001

# Frontend (Vite reads VITE_ prefixed vars). Safe to expose.
VITE_API_URL=http://localhost:3001